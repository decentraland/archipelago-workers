# Fixture bytes must survive every checkout unchanged: these are compared with the pack's
# manifest.json by sha256, and git's autocrlf would rewrite eight of the ten .bin files (they
# carry 0x0a and no NUL, so git reads them as text).
*.bin binary
* text=auto eol=lf
